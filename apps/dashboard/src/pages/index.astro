---
import Layout from '../layouts/Layout.astro';

const inviteUrl =
  import.meta.env.PUBLIC_BOT_INVITE_URL ??
  'https://discord.com/oauth2/authorize?client_id=YOUR_CLIENT_ID&scope=bot%20applications.commands&permissions=274878024704';
const supportUrl = import.meta.env.PUBLIC_SUPPORT_SERVER_URL ?? 'https://discord.gg/your-server';
---

<Layout title="Pawsitive — Professional Furry Discord Bot">
  <main class="page">
    <header class="nav-shell">
      <nav class="nav">
        <a href="/" class="brand">🐾 Pawsitive</a>
        <div class="nav-links">
          <a href="#features">Features</a>
          <a href="#workflow">Workflow</a>
          <a href="/login" class="nav-login">Dashboard Login</a>
        </div>
      </nav>
    </header>

    <section class="hero">
      <p class="eyebrow">PROFESSIONAL FURRY COMMUNITY PLATFORM</p>
      <h1>Build a legendary Discord server experience with style, safety, and soul.</h1>
      <p class="hero-copy">
        Pawsitive combines expressive interaction systems, image-rich command experiences, and moderation intelligence
        into one serious toolkit for furry communities.
      </p>
      <div class="cta-row">
        <a href={inviteUrl} target="_blank" rel="noopener noreferrer" class="btn btn-primary">Invite Pawsitive</a>
        <a href="/login" class="btn btn-glass">Open Dashboard</a>
      </div>
      <p class="meta">Powered by <strong>Seyfert</strong> · <strong>Elysia</strong> · <strong>Astro</strong></p>
    </section>

    <section id="features" class="panel-grid">
      <article class="panel tall">
        <h3>Advanced Image Interactions</h3>
        <p>From profile cards to scene generation workflows, create visual interactions members actually remember.</p>
        <span class="chip">Visual Engine</span>
      </article>
      <article class="panel">
        <h3>Smart Moderation Suite</h3>
        <p>Keep roleplay and social channels healthy with configurable guardrails and context-aware automations.</p>
        <span class="chip">Safety Controls</span>
      </article>
      <article class="panel">
        <h3>Guild Analytics</h3>
        <p>Track behavior trends, command usage, and engagement pulses to improve community retention.</p>
        <span class="chip">Insights</span>
      </article>
    </section>

    <section id="workflow" class="workflow">
      <h2>Launch in three clean moves</h2>
      <div class="steps">
        <div class="step"><strong>01</strong><span>Invite and authorize Pawsitive in your server.</span></div>
        <div class="step"><strong>02</strong><span>Sign in with Discord and pick your guild.</span></div>
        <div class="step"><strong>03</strong><span>Activate modules and tailor the vibe to your community.</span></div>
      </div>
      <a href={supportUrl} target="_blank" rel="noopener noreferrer" class="support-link">Need help? Join support server →</a>
    </section>
  </main>
</Layout>

<style>
  .page {
    max-width: 1100px;
    margin: 0 auto;
    padding: 24px 20px 72px;
  }

  .nav-shell {
    position: sticky;
    top: 10px;
    z-index: 20;
    margin-bottom: 48px;
  }

  .nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: rgba(12, 18, 34, 0.72);
    border: 1px solid var(--line);
    backdrop-filter: blur(12px);
    border-radius: 16px;
    padding: 12px 16px;
  }

  .brand { font-weight: 700; }
  .nav-links { display: flex; gap: 18px; color: var(--ink-300); }
  .nav-login { color: var(--ink-100); }

  .hero h1 { font-size: clamp(2rem, 5vw, 4rem); line-height: 1.06; margin: 12px 0 20px; }
  .hero-copy { max-width: 740px; color: var(--ink-300); font-size: 1.1rem; }
  .eyebrow { color: var(--brand-2); font-weight: 600; letter-spacing: 0.08em; font-size: 0.82rem; }
  .meta { margin-top: 16px; color: var(--ink-500); }

  .cta-row { display: flex; gap: 12px; margin: 28px 0 4px; flex-wrap: wrap; }
  .btn {
    padding: 12px 18px;
    border-radius: 12px;
    font-weight: 600;
    border: 1px solid transparent;
    transition: transform .2s ease, opacity .2s ease;
  }
  .btn:hover { transform: translateY(-1px); }
  .btn-primary { background: linear-gradient(130deg, var(--brand), var(--accent)); color: white; }
  .btn-glass { background: rgba(255,255,255,.05); border-color: var(--line); }

  .panel-grid {
    margin-top: 48px;
    display: grid;
    grid-template-columns: repeat(12, 1fr);
    gap: 14px;
  }
  .panel {
    grid-column: span 6;
    background: linear-gradient(180deg, rgba(255,255,255,.06), rgba(255,255,255,.02));
    border: 1px solid var(--line);
    border-radius: 16px;
    padding: 20px;
  }
  .panel.tall { grid-column: span 12; }
  .panel p { color: var(--ink-300); margin-top: 8px; }
  .chip {
    display: inline-block;
    margin-top: 16px;
    color: #09111f;
    background: linear-gradient(120deg, var(--brand-2), #9df1df);
    border-radius: 999px;
    padding: 5px 10px;
    font-size: 12px;
    font-weight: 700;
  }

  .workflow { margin-top: 48px; padding: 26px; border: 1px solid var(--line); border-radius: 16px; background: rgba(13, 19, 36, .65); }
  .workflow h2 { margin-bottom: 14px; }
  .steps { display: grid; grid-template-columns: repeat(3, minmax(0,1fr)); gap: 12px; }
  .step { background: rgba(255,255,255,.03); border: 1px solid var(--line); border-radius: 12px; padding: 14px; display: grid; gap: 8px; }
  .step strong { color: var(--brand-2); }
  .support-link { display: inline-block; margin-top: 16px; color: var(--brand-2); }

  @media (max-width: 820px) {
    .nav-links { display: none; }
    .panel, .panel.tall { grid-column: span 12; }
    .steps { grid-template-columns: 1fr; }
  }
</style>
